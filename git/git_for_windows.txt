Accept defaults except:

# The Windows command window can't be resized. It's awful to use.
# I think installing Git for use in Windows cmd also makes Git Bash 
# start in C:\Windows\system32 - hopeless!
Use Git from Git Bash only

# Best when collaborating with users on a different OS
Checkout windows-style, commit unix-style line endings

# Choose a Git Bash window which can be resized.
use MinTTY (the default terminal of MYSYS2)
